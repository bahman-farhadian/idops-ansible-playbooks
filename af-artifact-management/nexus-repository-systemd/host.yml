---
# Empty on purpose. Real hosts go in nexus_targets in a named *.local.yml
# file. playbook.yml adds them to the nexus_targets group at runtime.
# Do not put a real address, user, or password in this file.
all:
  children:
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
    nexus_targets:
      hosts: {}
