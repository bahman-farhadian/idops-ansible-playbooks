---
# Template project configuration
# Use this file as the single source of user-editable settings.

# -----------------------------------------------------------------------------
# Target connection
# -----------------------------------------------------------------------------
template_target_host: ""                 # SSH target host for this template project.
template_target_python_interpreter: ""   # Python path on target host.

# -----------------------------------------------------------------------------
# Example credentials (replace with project-specific keys)
# -----------------------------------------------------------------------------
template_operator_user: ""         # Initial operator username.
template_operator_password: ""     # CHANGE THIS PASSWORD BEFORE PRODUCTION.
template_operator_ssh_public_keys: [] # CHANGE THIS KEY BEFORE PRODUCTION.
# Example (was the live default; see local settings for real values):
# template_operator_ssh_public_keys:
#  - "ssh-ed25519 CHANGE_ME_WITH_YOUR_PUBLIC_KEY"

# -----------------------------------------------------------------------------
# Example project metadata
# -----------------------------------------------------------------------------
template_project_name: ""      # Human-readable project label.
template_environment_name: ""  # Environment marker. Values: dev | staging | production.
